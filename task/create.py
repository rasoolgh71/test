from django.shortcuts import render
'''
from django.http import HttpResponse
from .models import Athlete, Skill, Sport
from django import forms
class form_athlete(forms.Form):
    firstname =forms.CharField(max_length=30, label="firsname")
    lastename = forms.CharField(max_length=30,label="lastname")
    phonenumber = forms.CharField(max_length=50, label="phone number")
    namesport = forms.ForeignKey('Sport')
    birth_data = forms.DateTimeField(label="date of birthday", null=True, default=None, blank=True)
    visit_first_date = forms.DateTimeField(label=" visit first date", null=True, default=None, blank=True)
    male = forms.BooleanField(lebel="male")
    famale = forms.BooleanField(label="fmale")
    age = forms.IntegerField(label="age", default=0)
    skill = forms.ForeignKey('Skill')
    name_skill = forms.CharField(max_length=50, label="name skill")'''
